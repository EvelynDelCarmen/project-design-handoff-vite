import styled from 'styled-components';

const BigTitle = styled.h1`
  color: #F5F5F5;
  font-family: Poppins;
  font-size: 42px;
  font-style: normal;
  font-weight: 500;
  line-height: normal;
`;

export default BigTitle;
