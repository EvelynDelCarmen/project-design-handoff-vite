import styled from 'styled-components';

const BigCircularImage = styled.img`
  border-radius: 50%; /* Makes the image a circle */
  width: 100px; /* Adjust the width as needed */
  height: 100px; /* Adjust the height as needed to control the circle's size */
`;

export default BigCircularImage;
